/**Dependencies */
import { FC } from 'react';

/**Styles */
import './styles.scss';

type Props = {};

const Footer: FC<Props> = (props: Props) => {
  return <footer className="footer flex flex-jc-c">Built by Ocholla Tonny</footer>;
};

export default Footer;
